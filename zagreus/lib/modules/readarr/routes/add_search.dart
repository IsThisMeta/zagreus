import 'package:flutter/material.dart';
import 'package:zagreus/modules/readarr.dart';

class AddAuthorRoute extends StatelessWidget {
  const AddAuthorRoute({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Add Author')),
      body: const Center(child: Text('Add Author Search - Coming Soon')),
    );
  }
}
