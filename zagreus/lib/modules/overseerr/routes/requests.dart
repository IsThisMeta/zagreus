import 'package:flutter/material.dart';

class OverseerrRequestsRoute extends StatelessWidget {
  const OverseerrRequestsRoute({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Requests'),
      ),
      body: const Center(
        child: Text('Requests view coming soon'),
      ),
    );
  }
}
